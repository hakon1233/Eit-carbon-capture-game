# Sessions - 2026-06-27

## CAR-253 - Bug hunt & fix pass (Quality Loop)

Engineer agent, recurring quality pass. Dispatched 3 parallel bug-hunters over
carbon/emissions math, economy/projects, and win/state logic, then verified each
candidate against the actual code (excluding already-tracked bugs).

### Fixed directly (small, clearly-correct, no balance impact)
- **Win-gated achievements never unlocked** - `advanceOneMonth()` calls
  `checkAchievements()` (game.js:10984) before `checkWinLose()` (10987) sets
  `state.won`. Once `gameOver` is set the month loop halts, so `speed_run` and
  `against_all_odds` (both require `state.won`) could never fire. Fix: re-check
  achievements inside `checkWinLose()` once `gameOver` is set (idempotent).
  - Regression test: `scripts/test-win-gated-achievements.mjs` (structural +
    functional). Updated `test-sustained-win-loss.mjs` and
    `test-endgame-results-modal.mjs` to stub the new `checkAchievements()` call.
  - Verified: all 13 node scripts pass; `npm run build` green.

### False positive (verified, not filed)
- "speed_run year off-by-one" - Jan to Dec 2050 is exactly 12 streak months, so
  the win fires in Dec 2050 with `state.year === 2050`. Condition is reachable
  once the ordering bug above is fixed. No change.

### Filed as fix-issues (balance-affecting -> report-don't-fix)
- **CAR-266** [bug] `REGIONAL_PROJECT_CAPS` never enforced - UI uses
  `buildProjectWithEffectiveness` which lacks the cap check; only legacy
  `buildProject` checks. Whole table is dead code.
- **CAR-267** [bug] Event income multiplier scales project upkeep (upkeep
  subtracted before `*= getEventIncomeMultiplier()`), inverting cost during
  events and diverging from `calculateIncomeBreakdown` preview.
- **CAR-269** [bug] Embodied carbon always assumes 1 GW - construction record
  never stores `capacityGW`/`storageGWh`, so `|| 1` fallback always fires;
  battery storage also mis-united.

### Lower-confidence notes (not filed, recorded here)
- Project diminishing-returns/stacking applied only in HUD preview
  (`calculateNetCO2Rate`), not in the simulated removal path - HUD/reality diverge.
- Ocean-saturation and vegetation-stress modeled twice (sink functions + feedback
  loops at identical thresholds), possibly double-counted.
- `getCarbonBalance()` cache not invalidated after direct `state.co2` mutations
  (volcanoes/disasters) - 1-tick lag in saturation response.

---

## Session - 05:29

**Objective:** CAR-243 copy fix for the research-center empty state.

### Work log
- Reproduced the dead-end guidance with a new regression script for the no-allies research-center branch.
- Replaced the nonexistent "Diplomacy tab" instruction with the current map-region plus Negotiate Alliance workflow.
- Verified with `node scripts/test-research-center-empty-state.mjs` and `node scripts/test-alliance-overview-co2-reduction.mjs`.

### Decisions
- Kept the fix to copy only; the underlying alliance workflow already exists in the selected-region panel.

### Files changed
- `game.js`
- `scripts/test-research-center-empty-state.mjs`

---

## Session - 05:28

**Objective:** Fix CAR-144 so failed saved-game loads show a friendly player notice.

### Work log
- Classified the bug as a startup saved-game restore failure: malformed or incompatible saved data currently falls through to a fresh game after only `console.warn`.
- Added a regression script for malformed saved-game data and confirmed it failed before the fix.
- Added a visible saved-game load notice, dismiss control, and scoped `loadGame()` catch wiring.
- Verified the targeted script, production build, and a live browser startup with malformed `localStorage`.

### Decisions
- Keep the fix local to saved-game load handling and avoid unrelated dirty worktree files.

### Files changed
- game.html
- game.js
- style.css
- scripts/test-save-load-failure-notice.mjs
- docs/sessions/2026-06-27.md

## CAR-48 — Upgrade Vite 5 → latest (clear esbuild dev-server advisory GHSA-67mh-4wv8-2f99)

**Context:** `esbuild ≤0.24.2` (GHSA-67mh-4wv8-2f99) pulled in transitively via `vite ≤6.4.2`. Installed was vite 5.4.x. Clearing it required a breaking major bump.

**Change:**
- `package.json`: `vite ^5.2.0` → `^8.1.0`; `npm install` (resolved to vite 8.1.0, rolldown-based).
- `vite.config.js`: no edits needed — only uses stable APIs (`base`, `build.outDir`, `rollupOptions.input`, `defineConfig`). Multi-page input map intact.
- Node v22.22.1 satisfies Vite 8's `^20.19 || >=22.12` requirement.

**Verification:**
- `npm run build` → green, all 9 HTML entries + assets emitted, built in ~12s.
- `npm run preview` → base path `/Eit-carbon-capture-game/` index 200, game.html 200, hashed JS assets 200.
- `npm run dev` → server ready on Vite 8, base path index + game.html 200.
- `npm audit` → **0 vulnerabilities**; esbuild/vite advisory cleared.

**Files touched:** `package.json`, `package-lock.json`. Single-commit, revertible. No game logic changes.

## CAR-251 — Security & dependency hygiene audit (recurring quality pass)

**Scope:** Full product — client code (`game.js`), all HTML entries, build, deps, CI.

**Threat model:** Fully client-side single-player static game (Vite 8). No backend/network/secrets/auth/PII, no URL-param or free-text input → SSRF/CORS/redirect N/A; `localStorage` save is self-XSS-only.

**Checks:** `npm audit` full → **0 vulns** (deps all dev-only). No `eval`/`new Function`/`document.write`/`insertAdjacentHTML`/`outerHTML`. No `fetch`/XHR/WebSocket/`postMessage`. No secrets in source. CI `deploy.yml` safe (no `pull_request_target`, least-privilege perms, pinned actions). `npm run build` → green.

**Finding F1 (Low) — FIXED:** 14 external `target="_blank"` links across `Game_Data_Reference.html` (10) + `Emissions_Data_Reference.html` (4) lacked `rel`. Added `rel="noopener noreferrer"` (reverse-tabnabbing + referrer-leak hardening). Verified in `dist/`, build green. HTML-attribute-only, fully reversible.

**Not re-filed (already tracked):** CSP defense-in-depth (backlog), Vite 8 bump (in_review/CAR-48, landed), npm-audit dev advisories (done).

**Findings doc:** `docs/security-audit-CAR-251.md`. **Files touched:** `Game_Data_Reference.html`, `Emissions_Data_Reference.html`, findings doc, this log.

**Disposition:** Done.
