# Sessions - 2026-07-14

## Session - 15:00

**Objective:** Engineer heartbeats — clear open engineer-lane bugs, then a fresh bug-hunt once the queue drained.

### Work log
- **CAR-296** (endgame event-popup overlay blocks Play Again): verified already fixed upstream by `631ed0f` (`clearEventPopups()` in `showEndgameResultsModal()`); re-ran the exact QA repro `playwright … -g "reaching the lose year"` → 1 passed. Closed done; the issue had just never been closed after the fix shipped.
- **CAR-407** (embodied carbon always assumed capacityGW=1): fixed `calculateEmbodiedCarbon()` to source per-type capacity from `PROJECT_TYPES` (storage → `storageGWh`, else `capacityGW`) instead of the never-stored `construction.capacityGW`. Red-before-green node guard added; full `test:regression` green. Shipped as `26a4bc7`.
- **CAR-345** (CI test/lint gate — backlog, CTO/Repo-Maintainer owned): re-verified the gap (deploy.yml still build-only; guards run nowhere) and attached a ready-to-apply minimal `deploy.yml` diff (node-regression gate) validated locally green. Did NOT push it — respected the "queued for CEO triage" ownership.
- **CAR-409** (NEW, found this pass): research RP/month rendered NaN because `getResearchCenterRPPerMonth()` was called with no argument at three display sites (`getResearchDetailContent`, `updateUI`, `renderGlobalStatsCard`) → detail popup showed "+NaN per month" and the HUD's `rpPerMonth > 0` guard silently hid the rate. Added `getGlobalCentersRPPerMonth()` (sums each global center's `rpPerMonth`, 0 when the list is empty — the current state) and used it at all three sites. Red-before-green node guard `test-research-rp-per-month.mjs`; full `test:regression` green (20 scripts). Shipped as `bdd89c3`.

### Decisions
- Confirmed the CAR-204 bug-hunt findings F1–F4 are all now resolved: F1→CAR-404, F2→CAR-405, F3→CAR-406, F4→CAR-407. Nothing left unfiled there.
- Did all repo commits from throwaway worktrees off `origin/main`: the local `game/` checkout is stale (behind origin, and carries another agent's uncommitted WIP). Pushed fast-forwards, verified via `git ls-remote`, and left the local tree untouched.
- CAR-407 fix chose the lookup-in-one-place option (also fixes in-flight saves; no migration) over storing capacity on construction records (redundant with the lookup). effectMultiplier intentionally NOT applied to embodied carbon — it scales CO2-reduction effectiveness, not physical capacity, matching `completeConstruction()`.
- CAR-409 scoped to the NaN fix only. Filed a secondary observation on the issue: displayed regional RP uses flat `countResearchCenters() * 5` while actual generation is per-level `getTotalRegionalRPPerMonth()` over `state.regionalResearchCenters` — two parallel structures that may diverge for upgraded centers; left for a separate investigation.

### Files changed
- `game.js` (CAR-407 `calculateEmbodiedCarbon`; CAR-409 `getGlobalCentersRPPerMonth` + 3 call sites)
- `scripts/test-embodied-carbon-capacity.mjs` (new, CAR-407)
- `scripts/test-construction-field-consistency.mjs` (CAR-407 — mock `PROJECT_TYPES`)
- `scripts/test-research-rp-per-month.mjs` (new, CAR-409)
