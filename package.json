{
  "name": "carbon-capture-game",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test:regression": "for f in scripts/test-*.mjs; do echo \"» $f\"; node \"$f\" || exit 1; done",
    "test:e2e": "playwright test",
    "test": "npm run test:regression && npm run test:e2e"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.5.0",
    "@commitlint/config-conventional": "^20.5.0",
    "@playwright/test": "^1.61.0",
    "husky": "^9.1.7",
    "vite": "^8.1.0"
  }
}
